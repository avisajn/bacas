import $render_label from './render_label'

import $render_sic from './reder_label_sic'

import $render_sap from './render_label_sap'

export default {
  jne: $render_label,
  sic: $render_sic,
  sap: $render_sap
}